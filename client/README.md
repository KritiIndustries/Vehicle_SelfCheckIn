# Client — Vehicle SelfCheckIn

This folder contains the React client for Vehicle SelfCheckIn.

For backend API documentation, see:

- ../Backend/docs/API.md
- ../Backend/docs/openapi.yaml

Use the Postman collection at `../Backend/docs/postman_collection.json` to exercise the API.
